# 第8章 监督微调 SFT — 让模型学会对话

## 学习目标

掌握 SFT 训练：加载预训练权重 → 用对话数据微调 → 模型学会指令跟随。

## SFT vs Pretrain 关键差异

| 维度 | Pretrain | SFT |
|------|----------|-----|
| 目标 | 学习语言通用表示 | 学会指令跟随/对话 |
| 数据 | 纯文本 `{"text": "..."}` | 对话 `{"conversations": [...]}` |
| Loss | 所有 token | **仅 assistant 回复**（user 区域 mask=-100） |
| 学习率 | 5e-4 | **1e-5**（1/50，避免遗忘） |
| 序列长度 | 340 | **768**（对话更长） |
| 初始权重 | 随机 | `pretrain_*.pth` |
| 保存名 | `pretrain_*.pth` | `full_sft_*.pth` |

## Label Masking 原理

SFT 的核心：**只在 assistant 回复 token 上计算 loss**。

```
输入: <|im_start|>user\n你好<|im_end|>\n<|im_start|>assistant\n你好很高兴为你服务<|im_end|>\n
Label: -100  -100    -100  -100  -100    -100        你 好 很 高 兴 为 你 服 务 <|im_end|> -100
       \______________________ prompt ________________________/\________ response _________/
```

- prompt 区域 label=-100（CrossEntropy 的 ignore_index）
- response 区域 label=真实 token id
- pad 区域 label=-100

## train_epoch 结构

与 pretrain 相同的 9 步循环：
1. 动态 lr（余弦退火，base_lr=1e-5）
2. AMP 前向
3. `loss = output.loss + output.aux_loss`
4. 梯度累积 → unscale → clip → step
5. 定期日志 + 保存

差异仅在超参数和数据集。

## SFTConfig

| 参数 | 默认值 | 说明 |
|------|--------|------|
| learning_rate | **1e-5** | 比预训练低 50 倍 |
| max_seq_len | **768** | 对话数据更长 |
| from_weight | **pretrain** | 加载预训练权重 |
| save_weight | **full_sft** | 保存名 |

## 生成验证

SFT 后通过 `apply_chat_template` + greedy decode 验证：
```python
messages = [{"role": "user", "content": "你好"}]
prompt = tokenizer.apply_chat_template(messages, tokenize=True, add_generation_prompt=True)
output = model.generate(input_ids)  # 贪心解码
```

## 验证

```bash
pytest tests/m08_sft/ -v   # 17 个测试全绿
```
