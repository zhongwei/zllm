# AGENTS.md

本文件记录在本仓库中工作的 agent（以及人类协作者）必须遵守的约定，避免重复踩坑。

## 数学公式（LaTeX / Math）书写规范

GitHub 使用 MathJax 渲染 Markdown 中的 LaTeX 数学公式。官方文档：
<https://docs.github.com/en/get-started/writing-on-github/working-with-advanced-formatting/writing-mathematical-expressions>

以下是历次踩坑总结出的硬性规则。写作或修改 `docs/book/` 下任何含数学公式的 `.md` 文件时**必须**逐条遵守。

### 规则 1：禁用 LaTeX 细距 / 粗距命令 `\,` `\;` `\:` `\!`

这些命令在 GitHub 上**不可靠**，会导致公式渲染异常或显示为原始字符。

| 禁用 | 改用 |
|------|------|
| `\,`（thin space） | 普通半角空格 ` ` |
| `\;`（thick space） | 普通半角空格 ` ` |
| `\:`（medium space） | 普通半角空格 ` ` |
| `\!`（negative space） | 删除 |

` \quad ` 和 ` \qquad ` 是可靠的，可继续使用。

### 规则 2：禁止在块级公式 `$$ ... $$` 的 `\text{}` 里嵌套 `$ ... $`

错误示例（GitHub 上几乎必坏）：

```
$$
\boxed{ \text{$\mathbf{y}$ 在 $\mathbf{x}$ 方向上的投影} }
$$
```

正确做法：用 `\text{中文}` 把中文片段分段包起来，数学符号留在数学模式里：

```
$$
\boxed{ \mathbf{y}\text{ 在 }\mathbf{x}\text{ 方向上的投影} }
$$
```

### 规则 3：中日韩标点后接 `$` 时，必须先加一个半角空格

`、` `，` `。` `；` `：` `）` `」` 等中日韩全角标点**直接**贴着 `$`（行内公式起始符）时，GitHub 可能识别不出数学定界符。

| 禁用 | 改用 |
|------|------|
| `、$\mathbf{x}$` | `、 $\mathbf{x}$` |
| `，$x$` | `， $x$` |

即：在 `$` 前面留一个普通半角空格。

### 规则 4：定界符选择

- 行内公式：默认 `$ ... $`。
- 当公式内容含 `$` 字符或与 Markdown 语法（如 `*` `_`）冲突时，改用 `` $` ... `$ ``（反引号语法）。
- 块级公式：默认 `$$ ... $$`（前后各空一行）。也可用 ` ```math ... ``` ` 代码块语法。

### 规则 5：检查清单

提交前用如下命令自检（在仓库根目录运行）：

```bash
# 1. 是否残留 \, \; \: \!
grep -rnE '\\[,:!]' docs/book/

# 2. 是否在 \text{} 内嵌套了 $
grep -rn '\\text{[^}]*\$' docs/book/

# 3. 中日韩标点后是否直接接 $
grep -rnP '[、，。；：）」』]\$' docs/book/
```

三条命令均应无输出（或仅匹配无关的代码块内容）。
